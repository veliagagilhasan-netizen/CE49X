version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: ce49x-postgres
    environment:
      POSTGRES_USER: ce49x
      POSTGRES_DB: conflict_monitoring
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
